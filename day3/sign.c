#include<stdio.h>
int main()
{
    int x;
    printf("Ënter a number to check positive or negative\n");
    scanf("%d",&x);
    if (x>0)
    {
        printf(" %d is a +ve number\n",x );-9
    }
    else{
        printf(" %d is a -ve number\n",x );
    }
}